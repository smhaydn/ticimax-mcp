services:
  - type: web
    name: ticimax-mcp
    runtime: python
    buildCommand: pip install .
    startCommand: ticimax-mcp
    healthCheckPath: /health
    envVars:
      - key: MCP_TRANSPORT
        value: http
